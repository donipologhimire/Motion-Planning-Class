THE ATTACHED DOCUMENTATION HAS MORE DETAILED DESCRIPTION AND PICTURES FROM THE PROJECT

###################### FOR TRAPEZOIDAL DECOMPOSITION: ########################

############################# FOR BUG_1 ALGORITHM  ########################

STEP 1: CHECK IF PYTHON3 is installed in your computer 

IF NOT :

FOR MAC OS 

$ /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"
export PATH="/usr/local/opt/python/libexec/bin:$PATH"
export PATH=/usr/local/bin:/usr/local/sbin:$PATH
$ brew install python

$ python3

STEP 2 : INSTALL THESE LIBRARIES 

python -m pip install -U pip
python -m pip install -U matplotlib

pip install shapely
pip install numpy

if you run into some problem please, contact dghimire@uci.edu, hekstrom@uci.edu,shubhas4@uci.edu 


STEP 3: On Mac OS use finder to start a terminal. You can hit command+space and type terminal, then hit enter. 

STEP 4: Go to the location of the file and type: 
		python BUG1_TEAM1.py

PLease look at the documentation, which includes a section named platform used.
 


 